export default function FallbackHeader() {
  return (
    <header
      style={{
        padding: "16px",
        background: "#333",
        color: "white",
        height: 60,
        display: "flex",
        alignItems: "center",
      }}
    >
      <span>App</span>
    </header>
  );
}
